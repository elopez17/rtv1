#include <rtv1.h>

double	findintercone(t_ray ray, t_cone cone)
{
	if (ray.dir.x && cone.pos.x)
		;
	return (0);
}

double	findintercylinder(t_ray ray, t_cylinder cylinder)
{
	if (ray.dir.x && cylinder.pos.x)
		;
	return (0);
}
